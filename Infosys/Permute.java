package Infosys;

import java.util.*;

public class Permute {
    public static int[] permutation(int[] A) {
        int n = A.length;
        boolean found = false;
        for (int i = n - 1; i >= 0; i--) {
            if (A[i] < A[i + 1]) {
                int gr = i;

            }
        }
    }

}
